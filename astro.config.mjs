// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://kittyfromouterspace.github.io',
  base: '/algarve-property',
});
